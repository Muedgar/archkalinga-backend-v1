# Prevent platform-specific native binaries from being locked into
# package-lock.json when installing on macOS. This keeps the lockfile
# compatible with Linux (Docker / Railway) without needing --ignore-platform.
ignore-platform=true
